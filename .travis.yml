language: python
python:
  - "2.7"
  - "3.5"
# command to install dependencies
install:
  - pip install ipykernel "ipython<6"
  - ipykernel install --user
  - pip install -e .
  - pip install pytest
# command to run tests
script: py.test
